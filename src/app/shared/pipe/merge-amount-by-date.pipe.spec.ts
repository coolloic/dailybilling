import { MergeAmountByDatePipe } from './merge-amount-by-date.pipe';

describe('MergeAmountByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new MergeAmountByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
